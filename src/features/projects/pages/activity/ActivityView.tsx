import React from "react";

const ActivityView: React.FC = () => (
  <div style={{ padding: '2rem 0' }}>
    <h1 style={{ fontFamily: 'var(--font-heading, Geist)', fontSize: '1.5rem', color: 'var(--text-primary)', marginBottom: '0.5rem' }}>Activity</h1>
    <p style={{ color: 'var(--text-muted)', fontSize: '0.875rem' }}>Activity full view — coming soon</p>
  </div>
);

export default ActivityView;
